interface:
  display_name: "测试地图"
  short_description: "把测试套件整理成场景→情形→案例三层的单文件 HTML 报告，只读测试名不读实现"
  default_prompt: "使用 $test-map 生成这个项目的测试地图。"

# 用户已批准增量模式自动调用（见 decisions/test-map/2026-08-19-auto-incremental.md）；
# 首次建图仍需用户同意，触发与排除条件以 SKILL.md「调用前提」为准。
policy:
  allow_implicit_invocation: true
